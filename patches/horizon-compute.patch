from django.utils.translation import gettext_lazy as _

ENABLED = False  # Disable compute panel since we're using Oracle middleware

INSTALLED_APPS = [
    'openstack_dashboard.dashboards.project',
] 